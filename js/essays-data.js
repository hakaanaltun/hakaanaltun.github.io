---
---
/* essays-data.js — generated by Jekyll from post front matter.
   Site-hosted covers stay root-relative: every consumer (more-essays,
   series-nav, fragments-nav, cafe-nav) rewrites "/images/" to "/images/480/"
   for its thumb, and an absolute https://hakanaltun.io/… path made those
   thumbs resolve against production from any other host — so they were blank
   in local and preview builds. Off-site images (with a scheme) pass through. */
{% assign all_essays = site.posts | sort: "date" | reverse %}
var ALL_ESSAYS = [{% for essay in all_essays %}
{% assign essay_img = essay.thumb | default: essay.image | default: "" %}
  {href:{{ essay.url | split: "/" | last | jsonify }},title:{{ essay.title | jsonify }},subtitle:{{ essay.subtitle | default: "" | jsonify }},img:{% if essay_img contains '://' %}{{ essay_img | jsonify }}{% elsif essay_img != "" %}{{ essay_img | relative_url | jsonify }}{% else %}""{% endif %},date:'{{ essay.date | date: "%b %-d, %Y" }}',series:{{ essay.series | default: "" | jsonify }},seriesOrder:{{ essay.series_order | default: 0 }},coverStyle:{{ essay.cover_style | default: "" | jsonify }},coverLabel:{{ essay.cover_label | default: "" | jsonify }},story:{% if essay.story_card %}true{% else %}false{% endif %},storyUrl:{{ essay.story_url | default: "" | jsonify }}}{% unless forloop.last %},{% endunless %}{% endfor %}
];
